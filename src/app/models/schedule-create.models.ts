
export interface Schedule_create {
      date: Date;
      status: string;
      travel_time:string;
      load_status: boolean;
      unload_status: boolean
      arrival_time: string;
      departure_time: string;
      break_time: string;
      panier: boolean;
      grand_deplacement:boolean;
      work_location: string;
      validated: boolean;

 }

